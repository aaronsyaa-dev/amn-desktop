/**
 * LE NOM D'AJMANI — édition interne.
 *
 * Le nom de l'assistant est exclusif à AMN DevSec : le paquet Business n'a
 * pas à le connaître, et `check:business` le refuse dans le bundle livré.
 * Les textes de la Garde qui le prononcent portent donc un paramètre
 * `{ajmani}`, rempli ici — et jamais dans le dictionnaire partagé.
 */
export const NOM_DU_CHEF = 'Ajmani';
